"""Scrape lesson pages given lesson URLs stored in the DB.

This command is intentionally independent of the API client. It should read the
lesson URLs from the database and fetch each page to extract further metadata.
"""
print("scrape_lessons: not implemented yet — will iterate over DB lesson URLs and fetch pages")
